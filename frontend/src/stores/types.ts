/**
 * Core type definitions for Zustand stores
 *
 * This is a minimal setup ready to be extended with your application's state needs.
 * Add new slices and state as your application grows.
 */

/**
 * Combined store state
 *
 * Add your state slices here as your application needs grow.
 * Example:
 *
 * ```typescript
 * export interface StoreState {
 *   // Your state here
 *   count: number
 *   increment: () => void
 * }
 * ```
 */
export interface StoreState {
  // Placeholder - add your state here
  _initialized: boolean
}
